---
name: legal-case-workflow
description: Explicit-only workflow for one legal matter under this workspace's cases/ directory, covering source intake, evidence-bounded analysis, Chinese legal-document DOCX production, package QA, litigation drafting, and hearing preparation. Do not invoke for ordinary technical, database, Obsidian, generic PDF-conversion, or general legal-question tasks.
---

# 法律案件与文书工作流

本 Skill 是当前法律案件工作区的唯一案件入口。只有用户显式调用 `$legal-case-workflow`，并将任务指向 `cases/` 下的具体案件或材料包时，才启动本流程。

原 `$legal-document-docx` 的文书生成、排版和验收能力已并入本 Skill。不得再同时加载两套相互竞争的格式规则。

## 一、三层职责

1. **案件层**：材料接收、哈希登记、OCR 辅助、事实时间线、证据矩阵、争点与待核字段。
2. **文书层**：确定交付范围，选择模板或版本画像，生成、填充、修订法律文书。
3. **验收层**：核对事实、模板结构、语言状态、跨文书一致性、视觉排版、版本与哈希。

任何层都不得突破原始材料能够证明的事实边界。

## 二、启动前置动作

1. 确认唯一案件目录；没有案件目录时，先建立最小目录骨架，不扫描其他案件。
2. 用一句话声明本次模式、交付范围和所采用的格式权威。
3. 优先读取已有 `CASE_INDEX.md`、`HANDOFF.md`、`ISSUES.md` 与当前任务文件；哈希未变化时不重复全文处理。
4. 涉及 DOCX 时，必须完整读取 [document-production-protocol.md](references/document-production-protocol.md)。
5. 用户指定人工修正版或特定模板时，再读取相应版本画像；不得把画像中的案件事实复制到新案件。
6. 原始案件材料仅在本地处理；外部法律检索只使用去标识化摘要。

## 三、工作模式

- `intake-fast`：登记原始材料、文件类型、大小、SHA-256、可读性与处理状态。
- `analysis-focused`：制作时间线、事实矩阵、证据矩阵、争点与矛盾清单。
- `document-package`：按指定范围生成、修订并验收法律文书包。
- `hearing-prep`：围绕已核事实、争议焦点和证据缺口制作庭审提纲。
- `deep-review`：对高风险字段逐页回看并完成跨文书一致性复核。
- `update-incremental`：仅处理新增或哈希变化的材料并更新交接记录。

选择能完成当前请求的最窄模式。达到该模式交付条件后停止，不自动进入下一模式。

## 四、文书包范围

默认采用 `requested-only`，不得因参考包含有更多文件就自动扩张交付范围。

- `requested-only`：仅制作用户逐项指定的文书。
- `filing-core`：制作提出当前申请通常必需的核心文书，但仍须结合办理机关与用户要求确认。
- `filing-complete`：制作经用户确认的完整办理材料包，可包含律所内部或配套材料。

用户明确列出文件清单时，该清单优先于画像中的“示例全套”。

## 五、案件目录与原始材料

建议目录：

```text
cases/<case-id>/
├── 00_source/           # 原始材料，只读
├── 01_manifest/         # 文件清单、哈希、处理状态
├── 02_working/          # OCR、转写、检索辅助
├── 03_analysis/         # 时间线、事实/证据矩阵、争点
├── 04_templates/        # 经确认的案件模板或画像副本
├── 05_drafts/           # 草稿与交付包
└── HANDOFF.md
```

- 不覆盖、重命名、压缩、旋转、裁切或 OCR 回写 `00_source/`。
- OCR、Markdown 和可搜索 PDF 只作检索辅助，不能替代原始页面。
- 姓名、主体、日期、金额、账号、证件号、条款号、否定词、签名、印章、手写内容和流水号必须回看原始页。
- 每项关键事实应能回溯至材料编号、页码或截图坐标。

## 六、证据状态

统一使用以下状态，不得混写：

- `已核事实`：原始材料直接支持。
- `当事人陈述`：仅由一方表述，尚无独立材料印证。
- `合理推断`：由已核事实推导，必须写明推理依据。
- `争议事实`：材料或双方说法冲突。
- `待核事项`：缺少材料或关键字段无法确认。

缺少材料的信息写入内部待核表；正式文书相应位置留空或使用模板允许的空白线，不把内部核验语言写进可签署终稿。

PDF 与扫描材料按 [pdf-evidence-protocol.md](references/pdf-evidence-protocol.md) 执行。

## 七、文书格式路由

格式权威按以下顺序确定：

1. 用户对本次交付的明确要求；
2. 办理机关表格或用户提供的当前模板；
3. 经确认的律所模板或书面格式规范；
4. 用户明确选用的版本画像；
5. 无模板时的通用法律文书基线。

人工修正版画像位于 [qintong-enforcement-human-v20260828.md](references/profiles/qintong-enforcement-human-v20260828.md)。只有用户选择、引用或要求学习该版本时才应用。

律所级稳定字段集中维护在 [firm-constants.md](references/firm-constants.md)，不得把某一案件的当事人、案号、金额、日期、电话或代理方案固化为通用规则。

默认编辑强度为 `fill-only`：保留模板结构、条款、编号、选项和签署区，仅填入材料能够支持的内容。删除条款、改动计费选项、重编号、改变表格结构或扩张权利义务属于 `rewrite-authorized`，须有用户明确授权。

## 八、文书制作

1. 先列出：文书名称、事实来源、模板来源、待核字段、是否允许结构改写。
2. 将模板复制到 `05_drafts/` 后编辑，保持原模板与回滚版不变。
3. 优先进行最小化填充；不要为了“统一风格”改写有法律含义的固定条款。
4. 正式文书使用可直接签署、提交的语言；分析备注、证据缺口和处理过程放在独立内部文件。
5. 同一交付包中的主体名称、身份、地址、案号、金额、代理权限、落款日期和页数描述必须一致。
6. 页数、日期、利息等动态字段在最终渲染后重新计算或刷新。

## 九、六道验收门

1. **事实门**：关键字段均有材料来源；待核字段未被猜测补全。
2. **模板门**：没有未经授权的删条、重编号、改表格或更换签署结构。
3. **终稿语言门**：签署版不含 `【待确认】`、`传票载`、OCR、哈希、模型说明等内部过程语言。
4. **一致性门**：主体、案号、地址、金额、权限、日期和页数跨文书一致。
5. **视觉门**：逐页渲染检查分页、孤行、表格溢出、签署区、页眉页脚、印章/签名位置。
6. **版本门**：保存回滚版，记录最终文件 SHA-256；如提交前文件发生变化，重新验收。

运行结构化验收：

```bash
python scripts/validate_document_package.py \
  --package-dir <交付目录> \
  --profile references/profiles/qintong-enforcement-human-v20260828.json \
  --final
```

案件目录验收：

```bash
python scripts/validate_case.py --case-dir <案件目录>
```

自动检查通过不等于视觉检查通过；两者均完成后才可交付。

## 十、上下文与版本管理

- 原始 OCR、终端日志和批量中间结果写入文件，不在主对话堆积。
- 每个阶段完成后更新 `HANDOFF.md`：已完成、未完成、待核字段、失败项、采用的模板/画像版本、下一步。
- 模板、画像和律所常量都必须标注来源、版本或核验日期；出现冲突时报告冲突并选择本次权威来源，不静默混用。
- 对每次正式交付保留“输入版本—模板版本—输出哈希—验收结果”的最小链路。

## 十一、子代理

默认不启用。只有独立、只读、可并行的材料阅读任务才可考虑，且遵守 [subagent-policy.md](references/subagent-policy.md)。最终事实合并、法律判断和文书定稿由主代理完成。

## 十二、完成标准

最终报告必须明确列出：

- 已生成或修改的文件及路径；
- 本次交付范围与采用的模板/画像版本；
- 已完成的结构、视觉和哈希检查；
- 未完成事项、待核字段和转换失败项；
- 可回滚版本位置。

“命令已运行”或“文件已生成”均不等同于任务完成。
