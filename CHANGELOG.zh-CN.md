# 更新记录

## v1.1.0 - 2026-08-29

### 新功能

- 将 Skill 改造为 Codex、Kimi Code、WorkBuddy 和 CodeBuddy Code 共用的案件工作流。
- 增加跨平台显式调用入口，同时保持“只有用户明确点名后才启动完整案件流程”的边界。
- 增加跨平台兼容协议，统一安装目录、调用方式、权限边界和运行时降级规则。
- 增加通用 ZIP 打包器，使 `SKILL.md` 位于压缩包根目录，可直接用于 WorkBuddy 本地技能包导入。
- 在通用包内生成 `MANIFEST.sha256`，用于验证发布包文件完整性。

### 兼容性

- 保持标准 Agent Skills 目录结构，不为不同智能体复制案件、证据或文书规则。
- 保留 `agents/openai.yaml` 作为 Codex 专用增强，Kimi Code 与 WorkBuddy 可安全忽略。
- 不在共用 frontmatter 中写入平台专有工具名，避免 `allowed-tools` 等字段造成跨运行时冲突。
- Kimi Code 使用普通 Skill 而非 Flow Skill，以保留材料缺口、人工核对和视觉验收等停止条件。

### 质量控制

- 明确“Skill 已加载”不等于案件验收或文书验收通过。
- 缺少 Python、DOCX 渲染器或外部检索能力时，必须记录具体失败项，不得静默降低六道验收门。

## v1.0.1 - 2026-08-28

### 文档

- 将仓库发布状态由私有调整为公开。
- 明确公开内容包含特定律所的格式基线字段，但不包含案件材料、证件号码、电话、银行账号或密钥。
- 补充公开可见性与授权许可之间的区别。

## v1.0.0 - 2026-08-28

### 新功能

- 合并案件处理与法律文书 DOCX 制作能力，建立统一的 `$legal-case-workflow` 入口。
- 增加案件证据、文书制作、格式验收三层工作架构。
- 增加 `requested-only`、`filing-core`、`filing-complete` 三种文书包范围。
- 增加正式终稿、准正式草稿和内部分析文件的语言状态区分。
- 增加默认最小填充与经授权结构改写的操作边界。
- 增加人工修正版格式画像及对应 JSON 验收规则。
- 增加 DOCX 文书包自动验收脚本。

### 质量控制

- 建立事实、模板、终稿语言、跨文书一致性、视觉排版和版本哈希六道验收门。
- 明确模板画像只证明格式，不证明案件事实。
- 明确原始材料只读、OCR 仅作检索辅助、关键字段必须回看原始页面。
- 增加律所稳定字段与案件动态变量的分离规则。

### 兼容性

- 原 `$legal-document-docx` 能力已并入本工作流。
- 旧名称应仅作为显式兼容入口，不再维护第二套格式规则。
